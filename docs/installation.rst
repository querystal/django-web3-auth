============
Installation
============

At the command line::

    $ easy_install django-web3-auth

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-web3-auth
    $ pip install django-web3-auth
